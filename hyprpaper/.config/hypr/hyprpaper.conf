# ~/.config/hypr/hyprpaper.conf

ipc = true
splash = false

wallpaper {
  monitor = DP-1
  path = ~/.config/backgrounds/shaded.png
  fit_mode = cover
}

# Optional: fallback for anything else (like HDMI-A-1)
wallpaper {
  monitor =
  path = ~/.config/backgrounds/shaded.png
  fit_mode = cover
}
